import { ConfettiClickDirective } from './confetti-click.directive';

describe('ConfettiClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfettiClickDirective();
    expect(directive).toBeTruthy();
  });
});
