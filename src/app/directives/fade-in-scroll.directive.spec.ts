import { FadeInScrollDirective } from './fade-in-scroll.directive';

describe('FadeInScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInScrollDirective();
    expect(directive).toBeTruthy();
  });
});
