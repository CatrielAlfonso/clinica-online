import { EspecialidadesArrayPipe } from './especialidades-array.pipe';

describe('EspecialidadesArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialidadesArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
